[build]
  command = "npm run build"
  publish = ".next"

[build.environment]
  NODE_VERSION = "22"

# Declared explicitly rather than left to Netlify's auto-detection. Without the
# runtime, Netlify publishes .next as plain files and every route 404s.
[[plugins]]
  package = "@netlify/plugin-nextjs"
